{
  "skill_id": "xtrl.packet-runner",
  "version": "0.1.3",
  "title": "Packet Runner (worktree + evidence)",
  "description": "Runs a packet contract in an isolated git worktree and emits evidence artifacts against a target repo.",
  "entrypoint": "scripts/run_packet.py",
  "inputs": {
    "contract_path": {
      "type": "string",
      "required": true,
      "description": "Path to the packet contract JSON (e.g., $CODEX_STATE/xtrl/packets/examples/packet-001-worktree-collab.json)"
    }
  }
}
